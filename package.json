{
  "name": "terraform-deploy-action",
  "version": "1.0.0",
  "description": "Atlantis-inspired Terraform plan/apply orchestrator for GitHub Actions",
  "main": "src/main.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist --source-map --license licenses.txt",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "js-yaml": "^4.1.0",
    "minimatch": "^9.0.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.js"]
  }
}
